{
  "name": "game-of-life",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build:wasm": "wasm-pack build wasm-engine --target web --out-dir pkg",
    "build": "bun run build:wasm && tsc && vite build",
    "test": "vitest run",
    "preview": "vite preview"
  },
  "dependencies": {
    "@vitejs/plugin-basic-ssl": "latest",
    "typescript": "latest",
    "vite": "latest"
  },
  "devDependencies": {
    "vitest": "latest",
    "wasm-pack": "^0.15.0"
  },
  "packageManager": "bun@latest"
}
